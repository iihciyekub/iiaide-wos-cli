{
  "name": "iiaide-wos-cli",
  "version": "0.3.67",
  "private": true,
  "description": "Task-oriented CLI for interacting with Web of Science, extracting WOS IDs and author data, and delivering reusable data packages.",
  "repository": {
    "type": "git",
    "url": "https://github.com/iihciyekub/iiaide-wos-cli.git"
  },
  "homepage": "https://github.com/iihciyekub/iiaide-wos-cli#readme",
  "bugs": {
    "url": "https://github.com/iihciyekub/iiaide-wos-cli/issues"
  },
  "files": [
    "bin/",
    "src/",
    "docs/",
    "README.md",
    "CHANGELOG.md"
  ],
  "bin": {
    "iiaide-wos": "bin/iiaide-wos.js"
  },
  "scripts": {
    "lint": "node --check bin/iiaide-wos.js && node --check src/iiaide-wos.js && node --check src/lib/io.js && node --check src/lib/interactive.js && node --check src/lib/terminal.js && node --check src/lib/update.js && node --check src/lib/wos-browser-export.js && node --check import/wos.js",
    "check": "npm run lint",
    "test": "node --test",
    "verify": "npm run check && npm test",
    "help": "node bin/iiaide-wos.js --help",
    "menu": "node bin/iiaide-wos.js menu",
    "prepublishOnly": "node -e \"console.error('Publishing to the npm Registry is disabled; publish a private GitHub Release instead.'); process.exit(1)\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "1.59.1"
  }
}
